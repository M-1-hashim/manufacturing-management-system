{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "prisma generate --schema prisma/schema.vercel.prisma && next build",
  "regions": ["fra1"],
  "functions": {
    "src/app/api/**/route.ts": {
      "maxDuration": 60
    }
  }
}
