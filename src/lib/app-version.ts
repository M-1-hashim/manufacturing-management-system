/*
 * نسخهٔ برنامه — منبع واحد برای نمایش در UI و پاسخ /api/system/db-info.
 * هنگام هر ریلیز، این عدد با package.json و installer.nsi هم‌زمان bump می‌شود.
 * (خواندن از package.json در بستهٔ دسکتاپ قابل‌اتکا نیست — سرور و app
 * دو package.json جدا دارند.)
 */
export const APP_VERSION = '1.0.7'
